// let str = "Andaz Kumar"
// console.log(str.length);
// console.log(str[0], str[5] , str[9]);

// Qs.1 prompt the user to enter their full name. Generate a username for them based on the input.
// start username with @, followed by their full name and ending with the fullname length.

let str2 = prompt("Enther the fullname");
let str1 ="@";
let str3 = str2.length;
// console.log(str1 + str2 + str3) or
console.log(str1 + str2 + str2.length);

